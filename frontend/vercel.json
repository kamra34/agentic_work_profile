{
  "buildCommand": "rm -rf node_modules && npm install && npm install @rollup/rollup-linux-x64-gnu --save-optional && npm run build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping default install'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
